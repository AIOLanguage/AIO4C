tmp Robot {

    + core Core

    new (+ core Core)
    {
        $core = core
    }

    fun calc() : abstract

    tmp Core : inner {

        - batteryPower Int

        fun activate() : abstract : protected

        sco init : protected {

            fun installProgram() : abstract

            fun installDrivers() : abstract

            fun allocateMemory() : private {}
        }

    }

}