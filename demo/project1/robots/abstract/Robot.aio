class Robot : abstract
    {
        val core Core

        Robot(core Core);

        fun calc() : abstract

        class Core : abstract : inner
            {
                var batteryPower Int

                scope attack : public
                    {
                        fun fire() : abstract : protected
                    }

                scope init : protected
                    {
                        fun installProgram() : abstract

                        fun installDrivers() : abstract

                        fun connectToGPS() : private;
                    }
            }
    }