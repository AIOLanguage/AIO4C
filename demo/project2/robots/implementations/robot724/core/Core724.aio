typelink
    {
        robots.abstract.core.Core::Core
        java.util.logging.Logger::Logger
    }

constant log Logger : private = Logger.getLogger(Core724::class.name)

typename Core724 <- Core
    {
        function install() : override
            {
                log.info('CORE-724 has installed!')
            }

        function activate() : override
            {
                log.info('CORE-724 has activated!')
            }
    }