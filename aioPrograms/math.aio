//Math.aio

~dec: << minuend , subtrahend <<
@subtraction
    >> minuend - subtrahend

~dec: << dividend , divider <<
@division
    im quotient = dividend / divider
    >> quotient

~dec: <<a, b<<
@plus >> a + b

~dec: <<x, y, z<<
@countDistance
    >> sqrt(x * x + y * y + z * z)